#!/usr/bin/env bash

while bluetoothctl devices | grep -q '^Device '; do
    bluetoothctl devices | awk '{print $2}' |
    while read -r mac; do
        bluetoothctl remove "$mac"
    done
done

exec "$HOME/.local/bin/.exec-bt-sc.py"
